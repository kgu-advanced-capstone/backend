spring:
  application:
    name: api
management:
  endpoints:
    web:
      exposure:
        include: "prometheus,health,info" # prometheus를 명시적으로 포함
  endpoint:
    prometheus:
      enabled: true
    health:
      show-details: always